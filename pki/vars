# Commenting out the DNSNAMES and IPADDRS lines forces the script to perform
# automatic discovery. Either way, the values here (or discovered) will need
# to match the actual server configuration, for example, the server must serve
# from "localhost", and the client must connect to "localhost".
DNSNAMES="localhost"
IPADDRS="127.0.0.1"

BITS=4096
EXPIRATION_DAYS=365
ORGANIZATION="Göteborg Bit Factory"
CN="Göteborg Bit Factory"
COUNTRY=SE
STATE="Västra Götaland"
LOCALITY="Göteborg"
